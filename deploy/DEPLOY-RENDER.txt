FLUXGRID — STABLE URL FOR AFFILIATES (Render.com)
=================================================

WHY: trycloudflare.com links CHANGE every restart. Bad for affiliates.
THIS: https://fluxgrid.onrender.com stays the SAME forever (or add your own domain).

ONE-TIME SETUP (~15 minutes)
-----------------------------

1. Create free account: https://render.com

2. Push code to GitHub (or use Render manual deploy):
   - Create repo "fluxgrid" on github.com
   - Upload the voidlink folder (or git push)

3. In Render Dashboard → New → Blueprint
   - Connect your GitHub repo
   - Render reads render.yaml automatically

4. Set Environment Variables in Render dashboard:
   ADMIN_PIN          = (your strong pin — same as server/.env)
   BITCOIN_TIP_ADDRESS = bc1qe2tgy9d9m5h0l5xk5j00k4lvgpr0ggc3jye4jt
   NODE_ENV           = production

5. Deploy. Wait ~5 min.

6. Your permanent affiliate link:
   https://fluxgrid.onrender.com

OPTIONAL: Custom domain on Render
   - Render dashboard → fluxgrid service → Settings → Custom Domain
   - Add your domain (must own it — Namecheap, etc. ~$10/year)
   - Stable https://yourdomain.com for affiliates

FREE TIER NOTES
---------------
- Spins down after 15 min idle — first visitor waits ~30 sec to wake
- Upgrade to $7/mo "Starter" for always-on (better for affiliates)
- 1GB disk included for messages/data

AFFILIATE LINK NEVER CHANGES once deployed.